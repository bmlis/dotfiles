{{- $choices := list "home" "work" -}}
{{- $profile := promptChoiceOnce . "profile" "Select profile" $choices -}}
[data]
profile = {{- $profile | quote }}

[hooks.read-source-state.pre]
command = ".local/share/chezmoi/.install-password-manager.sh"
